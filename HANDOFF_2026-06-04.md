# 2026-06-04 Handoff

> 한국어 요약: 이 파일은 다음 세션에서 바로 이어가기 위한 작업 인수인계 메모입니다. 영어 항목은 Codex가 빠르게 읽기 위한 구조이고, 한국어 설명은 사용자가 상황을 쉽게 확인하기 위한 주석입니다.

## Current State

한국어 설명: 현재 로컬 개발 환경은 Neon 운영 DB와 분리되어 있습니다. 로컬에서는 내 PC의 PostgreSQL `budget_dev` DB를 보고, 웹/운영은 기존 Render + Neon을 그대로 봅니다.

- Local backend is configured to use local PostgreSQL, not Neon.
- Local DB name: `budget_dev`
- Local DB URL is in `server/.env.local`:
  - `postgresql://postgres:postgres@localhost:5432/budget_dev?schema=public`
- `server/.env.local` is ignored by Git via root `.gitignore`.
- Production/web is still on the existing Render + Neon setup.
- Local app was verified working after restoring the Neon dump into local PostgreSQL.

## What Was Done Today

한국어 설명: 오늘 한 핵심 작업은 "로컬을 Neon에서 분리", "Neon 덤프를 로컬에 복원", "로컬 앱 동작 확인"입니다.

1. Confirmed Render API was responding:
   - 한국어: 운영 API가 죽은 상태가 아니라는 것을 확인했습니다.
   - `/api/health` returned `200`
   - `/api/auth-status` returned expected `401` with CORS headers when not logged in
   - `/api/login` preflight was normal

2. Separated local development from Neon:
   - 한국어: 로컬에서 서버를 켤 때 Neon 운영 DB를 건드리지 않도록 `server/.env.local`을 만들었습니다.
   - Created `server/.env.local`
   - Local development now points to `localhost:5432/budget_dev`
   - Existing `server/src/env.ts` refuses to start a development server against Neon unless `ALLOW_REMOTE_DB=true`

3. Created and initialized local PostgreSQL DB:
   - 한국어: 내 PC PostgreSQL에 `budget_dev`라는 테스트용 DB를 만들고 Prisma 구조를 적용했습니다.
   - PostgreSQL service: `postgresql-x64-18`
   - DB: `budget_dev`
   - Prisma schema was pushed successfully after allowing Prisma engine access

4. Restored local data from latest Neon dump:
   - 한국어: 운영 Neon에서 백업해둔 최신 덤프를 로컬 DB에만 복원했습니다. 운영 DB에는 복원하지 않았습니다.
   - Source dump: `backup/neon-current-before-restore/neon-current-20260604-171236.dump`
   - Restore target: local `budget_dev`
   - Restore completed without errors

5. Verified local data counts:
   - 한국어: DB 전체 행수와 화면에 보이는 개수가 다른 이유를 확인했습니다. 화면은 삭제되지 않고 검증된 거래만 "전체" 탭에 보여줍니다.
   - `"Transaction"` total rows: `2545`
   - Visible app "전체" count: `921`
   - `"ImportRow"` active counts:
     - `new`: `133`
     - `duplicate`: `1`
     - `invalid`: `2`
   - Hidden/import-history data explains why DB total is larger than UI count.

6. Verified local app:
   - 한국어: 백엔드와 프론트가 로컬에서 둘 다 뜨는 것까지 확인했습니다. 로그인 비밀번호는 Neon 덤프의 기존 비밀번호입니다.
   - Backend health: `http://localhost:5000/api/health` returned `200`
   - Frontend: `http://127.0.0.1:3000` returned `200`
   - Login uses the restored Neon password, not `admin123`, because the Neon dump restored the `Auth` table.

## Important Gotchas

한국어 설명: 다음에 이어서 작업할 때 실수하기 쉬운 부분입니다. 특히 운영 Neon에 위험한 명령을 치면 안 됩니다.

- Do not use `npm run dev` in `server` unless network access for `npx -y node@20` is available.
- 한국어: 서버는 일반 `npm run dev` 대신 아래 명령을 쓰는 것이 안전합니다. 일반 dev 스크립트는 `node@20`을 네트워크에서 받으려다 멈출 수 있습니다.
- Use this for local backend:

```powershell
cd server
npm run dev:system-node
```

- Use this for local frontend:
- 한국어: 프론트는 아래 명령으로 띄웁니다. 주소는 `http://127.0.0.1:3000`입니다.

```powershell
cd client
npm run dev -- --host 127.0.0.1
```

- If `psql` appears to hang, it is probably waiting for a password prompt.
- 한국어: `psql`이 멈춘 것처럼 보이면 대부분 비밀번호 입력을 기다리는 중입니다.
- Use:

```powershell
$env:PGPASSWORD='postgres'
```

- Add `-w` to `psql`, `createdb`, or `pg_restore` commands.

- Do not run destructive production DB commands:
  - 한국어: 운영 Neon DB에는 데이터 삭제/초기화 명령을 절대 바로 치지 않습니다.
  - Do not run `db push --force-reset` against Neon.
  - Do not restore local DB dumps into Neon unless explicitly intending to overwrite production data.
  - Do not drop production tables.

## Useful Commands

한국어 설명: 다음 세션에서 상태 확인할 때 바로 쓸 수 있는 명령들입니다.

Check local backend:

```powershell
Invoke-WebRequest -Uri http://localhost:5000/api/health -UseBasicParsing
```

Check local frontend:

```powershell
Invoke-WebRequest -Uri http://127.0.0.1:3000 -UseBasicParsing
```

Check local DB counts:

```powershell
$env:PGPASSWORD='postgres'
'SELECT count(*) AS total FROM "Transaction";' | & 'C:\Program Files\PostgreSQL\18\bin\psql.exe' -h localhost -U postgres -p 5432 -d budget_dev -w
'SELECT status, count(*) FROM "ImportRow" GROUP BY status ORDER BY status;' | & 'C:\Program Files\PostgreSQL\18\bin\psql.exe' -h localhost -U postgres -p 5432 -d budget_dev -w
```

Restore latest Neon dump to local DB only:

한국어 주의: 아래 복원 명령은 로컬 `budget_dev` 전용입니다. Neon 운영 DB에 쓰면 안 됩니다.

```powershell
$env:PGPASSWORD='postgres'
& 'C:\Program Files\PostgreSQL\18\bin\psql.exe' -h localhost -U postgres -p 5432 -d budget_dev -w -c 'DROP SCHEMA public CASCADE; CREATE SCHEMA public;'
& 'C:\Program Files\PostgreSQL\18\bin\pg_restore.exe' -h localhost -U postgres -p 5432 -d budget_dev --no-owner --no-privileges --clean --if-exists 'backup\neon-current-before-restore\neon-current-20260604-171236.dump'
```

## Next Work

한국어 설명: 내일 이어서 할 일은 "로컬에서 검증한 DB 구조 변경을 안전한 migration으로 정리하고, 운영 Neon에는 데이터가 아니라 구조만 반영하는 것"입니다.

1. Cleanly identify which local DB structure changes are intended for production.
   - 한국어: 로컬에서 바꾼 테이블/컬럼 중 운영에 실제로 필요한 것만 골라냅니다.
2. Convert the verified schema changes into Prisma migrations.
   - 한국어: Prisma migration 파일로 정리합니다.
3. Review migration SQL before touching Neon.
   - 한국어: Neon에 적용하기 전에 SQL 내용을 먼저 확인합니다.
4. Back up Neon before applying migrations.
   - 한국어: 운영 DB 변경 전에는 반드시 Neon 백업을 먼저 만듭니다.
5. Apply migrations to Neon with `prisma migrate deploy`.
   - 한국어: 검토된 migration만 운영 Neon에 적용합니다.
6. Deploy backend/frontend code after the production DB schema is ready.
   - 한국어: 운영 DB 구조가 준비된 뒤 서버/프론트 코드를 배포합니다.
7. Verify production:
   - 한국어: 배포 후에는 로그인, 거래 개수, import 탭, 감사 로그 등을 확인합니다.
   - `/api/health`
   - login
   - transaction counts
   - import staging tabs
   - audit log and restore behavior if relevant

## Conceptual Rule

한국어 설명: 가장 중요한 원칙입니다.

Production rollout should update structure only, not overwrite data.

한국어: 운영 반영은 기존 Neon 데이터를 덮어쓰는 것이 아니라, 필요한 테이블/컬럼/인덱스 같은 구조만 migration으로 추가/수정하는 것입니다.

Local test DB can be restored/reset freely. Neon production DB should keep its existing data, and only receive reviewed schema migrations.

한국어: 로컬 DB는 마음껏 초기화/복원해도 되지만, Neon 운영 DB는 기존 데이터를 유지해야 합니다.
