# 가계부 외부 사용자 POC 계획서

## 1. 목적

현재 **효굥봉 가계부**는 실제 사용자인 효·굥의 입력 방식, 데이터, 고정비
검증 흐름에 맞춰 완성되어 있다. 이 POC는 이를 다른 개인·가족이 5~10분 안에
체험하고, 자신의 사용 방식에도 맞는지 판단할 수 있게 하는 별도 데모 환경이다.

이 문서는 계획만 정의한다. 현 서비스의 화면, 실제 데이터, 배포 환경을 변경하지
않는다.

## 2. POC 범위

포함:

- 별도 데모 주소와 별도 데이터베이스
- 처음 진입할 때 구성원 이름을 1~4명까지 설정
- 사전에 준비된 가상 거래·자산·고정비 데이터
- 거래 조회, 필터, 자산, 고정비 추천 및 자동/수동 매칭 체험
- 데스크톱과 모바일 화면 지원
- 데모 데이터 초기화 기능 또는 주기적 자동 초기화

제외:

- 실제 금융 데이터 연동
- 개인 데이터의 영구 저장 또는 외부 공유
- 유료 결제·회원가입 체계
- 현 서비스의 효·굥 데이터 구조를 직접 변경하는 작업

## 3. 사용자 흐름

1. 데모 주소 접속
2. 안내 화면에서 구성원 수와 이름 입력
   - 예: `나 / 배우자`, `철수 / 영희`, `엄마 / 아빠 / 자녀`
3. 샘플 데이터가 해당 이름으로 즉시 표시됨
4. 아래 핵심 기능을 체험
   - 구성원별 거래 필터
   - 월별 지출·자산 현황
   - 고정비 후보 추천
   - 고정비 자동 매칭, 확인 필요 거래, 수동 매칭
5. 필요 시 `데모 처음부터 보기`로 본인 체험 데이터를 초기화

## 4. 구성원 일반화 원칙

현재 서비스에서 효/굥은 `member` 값으로 저장·표시된다. POC에서는 이 개념을
일반화한다.

- 내부 데이터 모델은 `memberId`와 `memberName`을 분리한다.
- 화면에는 사용자가 입력한 이름만 표시한다.
- 고정비 매칭은 반드시 동일 구성원 안에서만 수행한다.
- `공동`은 별도 구성원으로 자동 추가하지 않는다. POC 설정에서 사용자가 필요할
  때만 직접 추가한다.
- 샘플 데이터의 효/굥 값은 실제 데이터와 연결하지 않고, 데모 DB에서만 선택한
  이름으로 치환한다.

## 5. 데모 데이터 설계

개인정보가 전혀 없는 가상 데이터만 사용한다.

- 3개월치 거래: 카드·계좌·현금, 수입·지출, 분류된 거래·미분류 거래 포함
- 구성원별 생활비·교통비·식비 등 소비 패턴 차이 포함
- 고정비: 통신비, 보험, 구독, 관리비 등
- 고정비 검증 상태: 자동 매칭 완료, 수동 매칭 완료, 확인 필요, 중복 의심,
  매칭 데이터 없음 사례를 각각 1개 이상 포함
- 자산: 예금·현금·카드대금 등 최소한의 예시

## 6. 기술·배포 분리

| 구분 | 실제 서비스 | POC |
| --- | --- | --- |
| 주소 | 현재 운영 주소 | 별도 데모 서브도메인 또는 별도 프로젝트 |
| DB | 실제 가계부 DB | 샘플 전용 DB |
| 로그인 | 현재 관리자/조회 계정 | 데모 전용 제한 계정 또는 익명 체험 세션 |
| 데이터 초기화 | 하지 않음 | 체험 종료·정기 작업으로 초기화 |
| 오류 영향 | 실제 사용자 | POC에 한정 |

POC 배포 환경에는 운영 DB 연결 문자열, 운영 비밀번호, 운영 백업 파일을 절대
넣지 않는다.

## 7. 구현 단계

### 1단계 — 데모 기반 분리

- 별도 배포 프로젝트와 별도 PostgreSQL DB 생성
- 가상 데이터 시드 스크립트 작성
- 운영 환경 변수와 비밀값이 POC에 전달되지 않도록 CI 설정 분리

### 2단계 — 구성원 설정

- 최초 진입 구성원 설정 화면 추가
- 데모 세션에 구성원 이름을 저장
- 모든 POC 표시·필터·고정비 매칭에 설정 이름 적용

### 3단계 — 체험 안내

- 첫 화면에 3단계 체험 안내 표시
- 고정비 확인 필요/중복 의심 카드에서 후보 거래와 수동 확정 동작 제공
- `데모 데이터 초기화` 제공

### 4단계 — 보호·운영

- 정기 데이터 초기화(예: 매일 새벽) 또는 사용자별 임시 데이터 삭제
- 업로드 파일 용량·형식 제한
- 오류 모니터링 및 POC DB 백업 범위 결정
- 모바일/PC 회귀 테스트 추가

## 8. 완료 기준

- 운영 서비스 UI·실제 데이터에 영향이 없음
- 신규 사용자가 효/굥이라는 고정 명칭을 보지 않음
- 선택한 구성원 이름이 거래·고정비·필터에 일관되게 반영됨
- 고정비 매칭에서 서로 다른 구성원이 교차 매칭되지 않음
- 새로고침 또는 초기화 후에도 가상 데이터로 정상 체험 가능
- 데스크톱과 모바일 주요 흐름을 E2E 테스트로 확인

## 9. 구현 전 확인할 결정 사항

- POC를 공개 링크로 제공할지, 초대받은 사람만 접근하게 할지
- 구성원을 최대 몇 명까지 지원할지 (권장: 4명)
- 체험 데이터 저장 기간 (권장: 브라우저 세션 또는 24시간)
- 사용자가 실제 파일 Import까지 체험할 수 있게 할지
- POC가 만족스러울 경우 다사용자 서비스로 확장할지, 데모로만 유지할지

## 10. 예상 영향과 원칙

POC는 단순한 복사본보다 **운영 서비스와 완전히 분리된 제품 실험 환경**으로
구성한다. 따라서 POC 구현 자체는 현재 가계부의 UI·데이터·로그인 흐름을 바꾸지
않는다. 다만 향후 POC 결과가 좋고 다사용자 서비스로 확장하기로 결정하면, 그때
구성원 모델과 계정·가구 단위 데이터 분리를 별도 프로젝트로 진행한다.
