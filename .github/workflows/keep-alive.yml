name: Keep Alive
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Budget App
        run: |
          curl -I https://my-budget-app-nwm8.onrender.com/api/health
