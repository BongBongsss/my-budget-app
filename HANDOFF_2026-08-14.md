# 2026-08-14 로그인 확인 로딩 화면 개선

## 목적

자동 로그인 세션이 유효한 경우 로그인 화면이 잠깐 보였다가 메인으로 전환되는
현상을 없애고, 서버 콜드스타트 시에도 현재 상태를 자연스럽게 안내한다.

## 반영 내용

- 앱 최초 진입 시 `/api/auth-status`로 세션을 확인하는 동안 로그인 화면 대신 전용
  로딩 화면을 표시한다.
- 유효한 세션이면 로그인 화면을 거치지 않고 메인 화면으로 이동한다.
- 세션이 없거나 만료된 경우에는 확인이 끝난 후 기존 로그인 화면을 표시한다.
- 로딩 화면은 최초 표시 후 최소 **2초** 유지한다.
  - 서버 응답이 2초보다 느린 콜드스타트 상황에서는 응답이 완료될 때까지 유지한다.
- 표시 구성:
  - 일반적인 파란 원형 스피너
  - `로그인 정보 확인 중`
  - `잠시만 기다려 주세요`
- PC 문구 크기는 과도하게 크지 않도록 조정했고, 모바일 문구는 기존보다 약 30%
  확대된 상태를 유지한다.

## 로컬 미리보기

개발 모드에서만 쿼리 문자열로 로딩 화면 최소 표시 시간을 지정할 수 있다.

```text
http://localhost:3000/?authLoadingPreview=2000
```

운영 배포에서는 이 옵션을 무시하며, 실제 최소 표시는 2초다.

## 검증

- 클라이언트 테스트: 18개 통과
- 프로덕션 클라이언트 빌드: 통과
- UI는 로그인 확인 중 화면에만 추가되며, 가계부 본문·모바일 구성·기존 로그인
  양식에는 변경이 없다.

## 관련 커밋

- `a458dab` Add session loading screen
- `9f734e9` Refine session loading timing
- `4ed7b9e` Tune session loading duration
